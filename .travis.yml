language: elixir
elixir:
  - 1.4.5
  - 1.5.3
  - 1.6.1
otp_release:
  - 20.1
sudo: false
env:
  - MIX_ENV=test
script:
  - mix coveralls.travis
services:
  - postgresql
  - redis-server
before_script:
  - psql -c "CREATE DATABASE flippant_test;" -U postgres
addons:
  postgresql: "9.6"
